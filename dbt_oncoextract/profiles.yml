oncoextract:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'oncoextract') }}"
      pass: "{{ env_var('POSTGRES_PASSWORD', 'oncoextract_dev') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'oncoextract') }}"
      schema: public
      threads: 4
